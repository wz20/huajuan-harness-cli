# Knowledge Taxonomy

本文件是当前知识库唯一受控分类词表。Agent 负责根据主题和真实资料提出分类；Huajuan 禁止在分类标准未确认时写入正式知识。

```json
{
  "schema": "huajuan-taxonomy/v1",
  "status": "awaiting-agent-confirmation",
  "categories": [],
  "tags": [],
  "aliases": [],
  "classificationOrder": []
}
```

## 一级分类

等待 Agent 提出少量稳定、互斥边界清楚的一级分类。

## 标签标准

等待 Agent 定义受控标签、适用条件与禁用近义词。

## 别名与同义词

等待 Agent 把同义词映射到唯一规范名称。

## 分类判断顺序

等待 Agent 定义新内容进入知识库时的判断顺序。

## 未决问题

- 等待主题与结构确认。

## 变更记录

- 尚未完成首次分类确认。
